import { Metadata } from "next";

export const metadata: Metadata = {
    title: "Statistics - The Daily Scribble",
    description: "A blog platform built with Next.js",
    keywords: ["statistics, graphs"]
  };

export default function Page() {
    return (
        <p>Statistics</p>
    )
}